name: 'openfootball_databricks'
version: '1.0.0'
config-version: 2

profile: 'openfootball_databricks'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  openfootball_databricks:
    silver:
      +materialized: table
    gold:
      +materialized: table
