SELECT
    MATCH_ID,
    COMPETITION_NAME,
    MATCH_DATE,
    TEAM_HOME,
    TEAM_AWAY,
    SCORE_FT_HOME,
    SCORE_FT_AWAY,
    SOURCE_FILE,
    TEAM,
    OPPONENT,
    SCORER,
    MINUTE,
    IS_PENALTY,
    IS_OWN_GOAL,
    EVENT_SEQ,
    INGESTED_AT,
    _RUN_ID
FROM {{ source('bronze', 'GOALS_RAW') }}
