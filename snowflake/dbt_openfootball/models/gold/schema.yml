version: 2

models:
  - name: response_after_conceding
    description: Um registro por gol sofrido; flags de reacao e resultado final.
    columns:
      - name: match_id
        tests:
          - not_null
      - name: conceding_team
        tests:
          - not_null
      - name: goal_minute
        tests:
          - not_null
      - name: goals_scored_after_conceding
        tests:
          - not_null
          - dbt_utils.accepted_range:
              min_value: 0
              max_value: 20
              inclusive: true
      - name: final_result
        tests:
          - not_null
          - accepted_values:
              values: ['WIN', 'DRAW', 'LOSS']

  - name: league_reaction_summary
    columns:
      - name: league
        tests:
          - not_null
      - name: pct_reacted
        tests:
          - not_null
